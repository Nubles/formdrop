# FormDrop

Drop filled copies of the same PDF AcroForm and immediately download `responses.csv`. Files never leave the browser.

Open https://nubles.github.io/formdrop/ or open `index.html` in a browser. Drop PDFs, or click the page to pick files — the CSV downloads at once.

This is a static page: no server, no accounts, no build step.
